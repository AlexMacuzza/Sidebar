<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_buscar_mercancias_ventas"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MercanciasVentasFragment">
    <SearchView
        android:id="@+id/txtbuscar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="12dp"
        android:layout_marginVertical="8dp"
        android:background="@drawable/bg_search_pill"
        android:iconifiedByDefault="false"
        android:searchIcon="@drawable/ic_search_teal"
        android:queryHint="@string/hint_buscar_mercancia" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginEnd="10dp"
        android:background="@color/black" />
    <ImageButton
        android:id="@+id/btnmerc"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="@null"
        android:gravity="center"
        android:scaleType="centerCrop"
        android:src="@drawable/ventas"
        android:contentDescription="@string/desc_agregar_mercancia"
        tools:ignore="SpeakableTextPresentCheck" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_marginEnd="10dp"
        android:background="@color/black" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/idRecycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="vertical" />
</LinearLayout>
